name: get
description: GetX 状态管理精简版 - 高性能状态管理、响应式编程（Rx）和轻量级依赖注入。
version: 5.0.0-release-candidate-9.3.2
homepage: https://github.com/jonataslaw/getx

environment:
  sdk: ">=3.4.0 <4.0.0"
  flutter: ">=3.22.0"

dependencies:
  flutter:
    sdk: flutter
  web: ">=1.0.0 <2.0.0"

dev_dependencies:
  flutter_lints: ^5.0.0
  flutter_test:
    sdk: flutter
